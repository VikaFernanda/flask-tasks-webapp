version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "flask-webapp"
        LoadBalancerInfo:
          ContainerName: "flask-webapp"
          ContainerPort: 80